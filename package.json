{
  "name": "gmermaid",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.6.0",
  "scripts": {
    "lint": "oxlint packages",
    "fmt": "oxlint --fix packages",
    "test": "vitest run",
    "typecheck": "tsc -b && pnpm --filter @gmermaid/mcp typecheck"
  },
  "devDependencies": {
    "oxlint": "^1.0.0",
    "typescript": "^5.6.0",
    "vitest": "^3.0.0"
  }
}
